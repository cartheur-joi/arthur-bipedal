## Cartheur.Animals.Robot

Robotis Dynamixel wrapper library in C# for protocol 1.0 and 2.0 using Visual Studio. This library also contains functions to collate and correlate motor positions, movements, and animation scenarios.

This code is cross-platform.

## Usage

In the root folder, open a console and use `dotnet build` to compile. Use as a reference for applications.